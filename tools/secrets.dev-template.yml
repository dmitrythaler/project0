version: '3.9'

services:
  api:
    environment:
      API_PRIVATE_KEY:
      API_PUBLIC_KEY:
      # no or commented out API_SECRET_KEY - means cookies shouldbn't be encrypted
      # API_SECRET_KEY:
      MINIO_ROOT_PASSWORD:

  storage:
    environment:
      MINIO_ROOT_PASSWORD:

