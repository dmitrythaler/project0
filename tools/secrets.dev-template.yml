version: '3.9'

services:
  api:
    environment:
      API_PRIVATE_KEY:
      API_PUBLIC_KEY:
      # missed or commented out API_SECRET_KEY - means cookies shouldbn't be encrypted
      # API_SECRET_KEY:
      AWS_SECRET_ACCESS_KEY:

  storage:
    environment:
      MINIO_ROOT_PASSWORD:

